import { Routes } from '@angular/router';
import { RestClienteComponent } from './components/rest-cliente/rest-cliente';

export const routes: Routes = [
    {
        path:'',
        component: RestClienteComponent
    }
];
